from scapy.all import sniff, IP 

def packet_callback(packet):
    if packet.haslayer(IP):   # Ensures the packet is an IP packet
        print(f"Source IP: {packet[IP].src} -> Destination IP: {packet[IP].dst}")

sniff(prn=packet_callback, store=False)   # Set store to False to avoid memory issues
 


